import { UserLayout } from "../layouts";
import { LoginForm } from "../components";

export const Login = () => {
  return (
    <UserLayout>
      <LoginForm />
    </UserLayout>
  );
};

export default Login;
